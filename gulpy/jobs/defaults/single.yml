keywords:
    - single

parse:
    duration: get_duration
    nprocs: get_nprocs
    completed: is_completed
    version: get_version

    energy: get_total_energy
    structure: get_pymatgen_structure
